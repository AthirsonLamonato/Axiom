# ── Paçoca — dependências principais ──────────────────────────────────
# Modo texto completo: TTS, overlay, controle de sistema, plugins, busca.
# NÃO inclui dependências de voz/ML (veja requirements-voice.txt).
#
# Instale com: pip install -r requirements.txt
# Para modo mínimo (sem TTS/overlay): pip install -r requirements-minimal.txt

# ── Core ──────────────────────────────────────────────────────────────
pyyaml>=6.0
requests>=2.28
psutil>=5.9
scikit-learn>=1.3   # NLU classifier (camada 2); scipy é dependência transitiva
plyer>=2.1
duckduckgo-search>=3.9
schedule>=1.2

# ── Text-to-Speech (offline) ──────────────────────────────────────────
pyttsx3>=2.90

# ── Interface visual (overlay) ────────────────────────────────────────
PyQt6>=6.4

# ── Controle de sistema e input ───────────────────────────────────────
pyautogui>=0.9
keyboard>=0.13
pynput>=1.7

# ── Google Drive / Calendar (opcional — configure credenciais OAuth) ───
google-auth>=2.17
google-auth-oauthlib>=1.0
google-api-python-client>=2.80

# ── Opcionais — descomente conforme necessidade ───────────────────────

# Dashboard web local (abre em localhost:7755)
# fastapi>=0.100
# uvicorn[standard]>=0.22

# Clipboard (copiar/colar texto por voz)
# pyperclip>=1.8

# OCR de tela (requer também o Tesseract instalado no sistema)
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   Linux:   sudo apt install tesseract-ocr tesseract-ocr-por
# pytesseract>=0.3
# Pillow>=9.0

# Controle de volume Windows
# pycaw>=20181226
# comtypes>=1.1

# Controle de brilho Windows
# wmi>=1.5

# Coqui TTS (voz mais natural, ~1.5 GB de modelos)
# TTS>=0.22

# Speaker diarization (identificação de falantes, ~2 GB de modelos)
#   Requer token HuggingFace e aceitar termos em:
#   https://huggingface.co/pyannote/speaker-diarization-3.1
# pyannote.audio>=3.1

# ── Desenvolvimento ───────────────────────────────────────────────────
# pytest>=7.0
# black>=23.0
# ruff>=0.1

# Empacotamento como executável
# pyinstaller>=5.0
