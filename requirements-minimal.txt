# ── Paçoca — dependências mínimas ─────────────────────────────────────
# Suficiente para rodar: python main.py --mode text --no-tts --no-overlay
#
# Instale com: pip install -r requirements-minimal.txt

pyyaml>=6.0
requests>=2.28
psutil>=5.9
pydantic>=2.0       # obrigatório: ToolRegistry usa validação Pydantic
plyer>=2.1
duckduckgo-search>=3.9
schedule>=1.2
